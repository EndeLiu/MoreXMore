const server = "http://localhost:8510"
// const server = "http://237a8i4430.wicp.vip"

const config = {
  server,
  loginUrl: server + '/api/login',
  accountUrl: server + '/api/account'
}

export default config
